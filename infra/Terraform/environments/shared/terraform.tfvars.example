# No variables required as of the shared environment's current state — the
# github-actions-role is looked up via a data source (see main.tf), not
# managed by this Terraform config. Kept as a placeholder file so
# `cp terraform.tfvars.example terraform.tfvars` in the docs stays valid if
# variables are added back later.
