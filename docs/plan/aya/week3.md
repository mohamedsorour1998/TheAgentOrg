# Aya — Week 3 (Aug 22–27): the DORA table + backup video

Feature freeze **Tuesday Aug 25**. After that: only re-run metrics and fix
flakiness.

---

## Sat–Sun Aug 22–23 — run the DORA batch

**Task: run 10 tickets with no checks (Reem's baseline) vs 10 through the
Agent Org.** Collect change-failure rate, lead time, and how many bad changes
each path let through.
**Done when:** you have both columns of raw data saved (a CSV/JSON is fine).

---

## Mon Aug 24 — the comparison table

**Task: build the comparison table/chart for the deck.**
The headline: the Agent Org blocks the poisoned change **10/10**; the
baseline ships it. One clean visual, not a data dump.
**Done when:** the table is in the demo deck — the judges explicitly ask for
DORA metrics, this is worth real points.

---

## Tue Aug 25 — freeze + backup video

**Task: record the backup video today.**
A full clean + poisoned run, in **English**, in case the live demo hits
trouble at the venue.
**Done when:** the video plays start to finish without errors.

**Task: freeze at end of day.** From here: only re-run metrics and fix
flakiness. No new tests.

---

## Wed–Thu Aug 26–27 — final checks

**Task: re-run the DORA batch once more** after any late fixes from the rest
of the team, to make sure the 10/10 number still holds.
**Done when:** the numbers in the deck match a fresh run.

---

## End of week 3 — done when

- DORA comparison table is finished and in the deck.
- Backup video recorded, in English, plays cleanly.
- Numbers re-verified against the final state of the repo.
