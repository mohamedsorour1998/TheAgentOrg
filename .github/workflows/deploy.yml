# AgentCore deploy — build the five agent images, push to ECR, deploy runtimes.
# OWNER: Sorour.
#
# ZERO STATIC AWS KEYS. Every AWS step assumes github-actions-role through
# GitHub's OIDC provider, which already trusts
# `repo:mohamedsorour1998/TheAgentOrg:*` in account 339712964409. If a step here
# cannot authenticate, fix the role trust; do not add an AWS_ACCESS_KEY_ID
# secret. The absence of static keys is the point of this workflow, not a detail.
#
# WHY THE agentcore CLI RUNS HERE AND NOT ON A LAPTOP. It creates the runtime
# resources, so wherever it runs is what the account reflects. Run from a laptop,
# the five live runtimes correspond to one person's working directory and nobody
# can tell afterwards which commit produced them. Run from here, they correspond
# to a commit on main.
#
# THE PACKAGING DEPENDENCY IS LOAD-BEARING. pyproject must declare
# [tool.setuptools.packages.find] with include = ["agentorg*"]. With the older
# `packages = ["agentorg"]`, a non-editable install silently drops
# agentorg/agents, agentorg/common and agentorg/security -- the image builds
# green and dies on import. Measured on 2026-08-20 against main before that fix
# landed: `pip install .` then `import agentorg.agents` gives
# ModuleNotFoundError. The Dockerfile's import smoke test is what turns that
# from a runtime failure into a build failure, and the preflight job below
# refuses to build at all if the declaration regresses.

name: deploy

on:
  push:
    branches: [main]
    paths:
      - "agentorg/**"
      - "pyproject.toml"
      - ".github/workflows/deploy.yml"
  # Redeploying an unchanged commit -- after a runtime is deleted, or to recover
  # a crash-looping one -- is a real need the paths filter would otherwise block.
  workflow_dispatch:

# Two deploys racing would push the same tags and configure the same runtimes
# concurrently. Never cancel mid-deploy: a cancelled run can leave images pushed
# and runtimes half-configured, which is worse than a queued wait.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  AWS_REGION: us-east-1
  # Not secret: readable in the account and printed in plan output. Inline so a
  # reader can see what this workflow assumes without opening settings.
  AWS_ROLE: arn:aws:iam::339712964409:role/github-actions-role
  ECR_PREFIX: theagentorg-shared
  # Read from docs/plan/week1-verification-log.md, not re-derived from Terraform
  # state, per the task. Terraform also outputs it as agentcore_runtime_role_arn.
  RUNTIME_ROLE: arn:aws:iam::339712964409:role/theagentorg-shared-agentcore-runtime-role

jobs:
  # Cheap, credential-free, and it fails the run before any image is built if the
  # packaging declaration that the images depend on has regressed.
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # A real non-editable install into a clean venv, then imports from a
      # directory that is NOT the source tree. `cd /tmp` matters: from the repo
      # root, `import agentorg` finds ./agentorg whether or not the install
      # shipped it, so the check would pass against the broken declaration.
      - name: A non-editable install must ship every subpackage
        run: |
          python -m venv /tmp/wheelcheck
          /tmp/wheelcheck/bin/pip install --quiet .
          cd /tmp
          /tmp/wheelcheck/bin/python -c "
          import agentorg, agentorg.agents, agentorg.common, agentorg.security
          from agentorg.agents import server
          from agentorg.security import gitleaks_tool, semgrep_tool, trivy_tool
          import importlib.resources as r
          assert r.files('agentorg.security').joinpath('gitleaks.toml').is_file(), 'gitleaks.toml did not ship'
          assert r.files('agentorg.security').joinpath('semgrep_rules.yml').is_file(), 'semgrep_rules.yml did not ship'
          assert len(server.AGENTS) == 5, f'expected 5 agents, got {len(server.AGENTS)}'
          print('preflight OK: subpackages, data files and all five agents present')
          "

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    needs: preflight
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: deploy-build-${{ github.run_id }}

      # Proves OIDC worked before anything expensive starts, so an auth failure
      # reads as one rather than as a docker or ECR error.
      - name: Who am I
        run: aws sts get-caller-identity

      - uses: aws-actions/amazon-ecr-login@v2
        id: ecr

      # AgentCore runs arm64 and the runner is amd64, so this is required, not
      # optional. Without it the build produces an amd64 image that pushes and
      # deploys and then fails to start with an exec format error.
      - uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: docker/setup-buildx-action@v3

      # ONE image, five tags. Every agent runs the same code with a different
      # AGENT_ROLE, so building five identical images would multiply build time
      # by five for no difference in content. Tagged with the commit SHA as well
      # as latest, because `latest` cannot tell you which commit is running.
      - name: Build and push
        run: |
          set -euo pipefail
          registry='${{ steps.ecr.outputs.registry }}'
          sha='${{ github.sha }}'

          tags=()
          for agent in planner developer reviewer security sre; do
            repo="${registry}/${{ env.ECR_PREFIX }}-${agent}-agent"
            tags+=(--tag "${repo}:${sha}" --tag "${repo}:latest")
          done

          docker buildx build \
            --platform linux/arm64 \
            --file agentorg/agents/Dockerfile \
            --push \
            "${tags[@]}" \
            .

      # Reads back what ECR actually holds rather than trusting the push. A push
      # that half-succeeded would otherwise be discovered by the deploy job.
      - name: Verify every repository received the image
        run: |
          set -euo pipefail
          for agent in planner developer reviewer security sre; do
            repo="${{ env.ECR_PREFIX }}-${agent}-agent"
            if ! aws ecr describe-images \
                  --repository-name "$repo" \
                  --image-ids imageTag='${{ github.sha }}' \
                  --query 'imageDetails[0].imageDigest' --output text >/dev/null 2>&1; then
              echo "::error::$repo has no image tagged ${{ github.sha }}"
              exit 1
            fi
            echo "$repo: ${{ github.sha }} present"
          done

  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: build
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
          role-session-name: deploy-runtime-${{ github.run_id }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # The CLI is installed here rather than expected on a laptop. Version
      # printed so a run records which CLI produced its runtimes -- when the
      # NO agentcore CLI. It builds the image itself in CodeBuild, and that path
      # cannot work here: `agentcore configure` OVERWRITES
      # agentorg/agents/Dockerfile with a rendered template, and the build then
      # pulls its base image from Docker Hub anonymously. Measured across three
      # runs -- CodeBuild died every time with
      #
      #   429 Too Many Requests
      #   failed to resolve source metadata for docker.io/library/python:3.12-slim
      #
      # The tell that it was not our Dockerfile: our build job transferred 5.01kB
      # while CodeBuild transferred 4.07kB of the same path.
      #
      # bedrock-agentcore-control create-agent-runtime takes an ECR containerUri
      # directly, so the image the build job above already pushed -- arm64,
      # verified in the OCI index, tagged with this commit -- is what runs. No
      # second build, no Docker Hub, one artifact from build to runtime.
      #
      # AgentCore Runtime requires arm64 (AWS Graviton); an amd64 image deploys
      # and then fails to start. That is why the build job pins --platform.
      - name: Create or update the five runtimes from the ECR images
        run: |
          set -euo pipefail
          account="$(aws sts get-caller-identity --query Account --output text)"
          registry="${account}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com"

          # Existing runtimes are updated, absent ones created. Without this the
          # second deploy of any agent fails on a name conflict, and a workflow
          # that only works once is not a deploy pipeline.
          existing="$(aws bedrock-agentcore-control list-agent-runtimes \
            --query 'agentRuntimes[].[agentRuntimeName,agentRuntimeId]' --output text || true)"

          for agent in planner developer reviewer security sre; do
            name="theagentorg_${agent}"
            uri="${registry}/${{ env.ECR_PREFIX }}-${agent}-agent:${{ github.sha }}"
            echo "::group::${name}"

            id="$(awk -v n="$name" '$1 == n {print $2}' <<<"$existing" | head -1)"

            if [ -n "$id" ]; then
              aws bedrock-agentcore-control update-agent-runtime \
                --agent-runtime-id "$id" \
                --agent-runtime-artifact "{\"containerConfiguration\":{\"containerUri\":\"${uri}\"}}" \
                --role-arn "${{ env.RUNTIME_ROLE }}" \
                --environment-variables "AGENT_ROLE=${agent}" \
                --network-configuration '{"networkMode":"PUBLIC"}' \
                --query 'agentRuntimeArn' --output text
            else
              aws bedrock-agentcore-control create-agent-runtime \
                --agent-runtime-name "$name" \
                --agent-runtime-artifact "{\"containerConfiguration\":{\"containerUri\":\"${uri}\"}}" \
                --role-arn "${{ env.RUNTIME_ROLE }}" \
                --environment-variables "AGENT_ROLE=${agent}" \
                --network-configuration '{"networkMode":"PUBLIC"}' \
                --description "The ${agent} agent of The Agent Org, from ${{ github.sha }}" \
                --query 'agentRuntimeArn' --output text
            fi

            echo "::endgroup::"
          done

      # A runtime reports CREATING before it is usable, so this waits rather than
      # reading the status once and calling it done.
      - name: Wait for all five to reach READY
        run: |
          set -euo pipefail
          # shellcheck disable=SC2016  # the JMESPath backticks are literal, not
          # shell expansion: single quotes are exactly what this query needs.
          for attempt in $(seq 1 40); do
            out="$(aws bedrock-agentcore-control list-agent-runtimes \
              --query 'agentRuntimes[?starts_with(agentRuntimeName, `theagentorg`)].[agentRuntimeName,status]' \
              --output text)"
            echo "--- attempt ${attempt}"
            echo "$out"
            total="$(wc -l <<<"$out" | tr -d ' ')"
            ready="$(grep -c 'READY' <<<"$out" || true)"
            if [ "$ready" = "5" ]; then
              echo "::notice::all five runtimes READY"
              exit 0
            fi
            # Fail fast on a terminal state rather than waiting out the loop.
            if grep -qE 'CREATE_FAILED|UPDATE_FAILED|DELETING' <<<"$out"; then
              echo "::error::a runtime reached a terminal failure state"
              exit 1
            fi
            sleep 15
          done
          echo "::error::timed out waiting for five READY runtimes (last: ${ready:-0}/${total:-0})"
          exit 1

      # A RUNTIME REPORTS READY BEFORE ITS ENDPOINT WILL SERVE THE NEW VERSION.
      # Measured: the runtime-status wait passed at 23:59:21 and the invoke fired
      # inside the same second, giving
      #
      #   ResourceNotFoundException: No endpoint or agent found with qualifier
      #
      # with the correct ARN and --qualifier DEFAULT. A by-hand invoke of the same
      # runtime minutes later returned statusCode 200 and a real plan, its
      # endpoint's liveVersion having reached "2". So runtime READY is necessary
      # and not sufficient.
      #
      # THIS RETRIES THE INVOKE INSTEAD OF POLLING THE ENDPOINT. The obvious
      # version polled ListAgentRuntimeEndpoints until liveVersion appeared, and
      # that call is not grantable to this role: simulate-principal-policy returns
      # `allowed` for the action with no resource, and `implicitDeny` with the
      # runtime ARN or the runtime-endpoint ARN, matching NO statement, while the
      # policy grants bedrock-agentcore:* on "*". Two live runs confirmed the
      # deny. Retrying the thing we actually need is both simpler and a stronger
      # check: it asserts the endpoint can serve rather than that a status field
      # claims it can.
      - name: Invoke the planner until it returns a real plan
        run: |
          set -euo pipefail
          # `head -1` IS LOAD-BEARING. With `--output text`, this CLI emits the
          # ARN and then a literal "None" on a second line -- verified with od:
          #   ...theagentorg_planner-td4Fou4YjY \n N o n e \n
          # so the variable held a two-line value and every invoke failed with
          # ResourceNotFoundException. The retry loop then classified that as the
          # endpoint-promotion race and polled eleven times against a runtime
          # that was answering statusCode 200 to a by-hand call the whole time.
          # The stray "None" was visible in the job log directly under the ARN
          # and read as a diagnostic rather than as part of the value.
          arn="$(aws bedrock-agentcore-control list-agent-runtimes \
            --query "agentRuntimes[?agentRuntimeName=='theagentorg_planner'].agentRuntimeArn | [0]" \
            --output text | head -1)"
          echo "planner runtime: [${arn}]"

          # A malformed ARN must fail here, not as a retried "not serving yet".
          case "$arn" in
            arn:aws:bedrock-agentcore:*:runtime/theagentorg_planner-*) ;;
            *) echo "::error::resolved planner ARN is not a single valid ARN: [${arn}]"
               exit 1 ;;
          esac

          # Both of these were found by running the call by hand, not assumed:
          #   * --payload must be BASE64. Raw JSON gives `Invalid base64: "{...}"`
          #     before the request leaves the CLI.
          #   * --qualifier is required, or the call fails with
          #     ResourceNotFoundException even against a READY runtime.
          payload='{"run_id":"ci-smoke","ticket_id":"CI-1","ticket_text":"Add a health check endpoint."}'
          encoded="$(printf '%s' "$payload" | base64 | tr -d '\n')"

          for attempt in $(seq 1 20); do
            if err="$(aws bedrock-agentcore invoke-agent-runtime \
                  --agent-runtime-arn "$arn" \
                  --qualifier DEFAULT \
                  --payload "$encoded" \
                  --content-type application/json \
                  /tmp/invoke-out.json 2>&1)"; then
              echo "--- response"
              cat /tmp/invoke-out.json
              # Asserts on CONTENT, not exit status. A runtime returning 200 with
              # an empty body exits 0, and "the call succeeded" is exactly the
              # reassuring non-answer this project keeps having to distinguish
              # from a real completion.
              if grep -q '"tasks"' /tmp/invoke-out.json; then
                echo "::notice::planner returned a real plan on attempt ${attempt}"
                exit 0
              fi
              echo "::error::planner answered without tasks; not a real completion"
              exit 1
            fi

            # Only the endpoint-not-ready race is worth waiting out. Anything
            # else -- a denial, a bad ARN, a 500 from our own container -- must
            # fail now rather than be retried nineteen more times.
            if grep -q 'ResourceNotFoundException' <<<"$err"; then
              echo "--- attempt ${attempt}: endpoint not serving yet"
              sleep 15
              continue
            fi
            echo "::error::invoke failed for a reason retrying cannot fix: ${err}"
            exit 1
          done
          echo "::error::endpoint never began serving within the retry budget"
          exit 1

